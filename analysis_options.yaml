include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gen.dart"
    - "lib/l10n/gen/*.dart"

linter:
  rules:
    lines_longer_than_80_chars: false
    prefer_single_quotes: true
