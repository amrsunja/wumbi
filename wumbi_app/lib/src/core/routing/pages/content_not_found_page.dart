import 'package:flutter/material.dart';

class ContentNotFound404Page extends StatelessWidget {
	const ContentNotFound404Page({super.key});

	@override
	Widget build(BuildContext context) {
		return Scaffold(
			body: Center(
				child: Text('404'),
			),
		);
	}
}
